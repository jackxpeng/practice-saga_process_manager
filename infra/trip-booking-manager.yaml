apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip-booking-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trip-booking-manager
  template:
    metadata:
      labels:
        app: trip-booking-manager
    spec:
      containers:
      - name: trip-booking-manager
        image: trip-booking-manager:latest
        imagePullPolicy: Never
        env:
        - name: DATABASE_URL
          value: "postgresql://tripuser:trippass@postgres:5432/tripdb"
        - name: RABBITMQ_URL
          value: "amqp://guest:guest@rabbitmq:5672/"
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: trip-booking-manager
spec:
  ports:
  - port: 8000
  selector:
    app: trip-booking-manager
